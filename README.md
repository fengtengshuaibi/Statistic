
  # 数据统计图生成器

  This is a code bundle for 数据统计图生成器. The original project is available at https://affix-bobbin-03193411.figma.site

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
